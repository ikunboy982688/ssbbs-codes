# ssbbs-codes

搜书吧（ssbbs）高级版 App 的卡密激活清单。

- `codes.txt` 只存**已激活卡密的 SHA256 哈希**，不存明文卡密。
- App 每次启动会下载此文件，比对用户输入卡密的哈希是否在清单内。
- 要封禁某个卡密：删掉对应那一行 hash 并 commit 即可（App 下次启动自动收回授权）。
- 此仓库为公开只读数据源，**不要往里写任何明文卡密或密钥**。
